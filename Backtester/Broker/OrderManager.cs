using System;

namespace Backtester.Broker
{
    /// <summary>
    /// Manages the lifecycle and queuing of pending orders.
    /// </summary>
    public class OrderManager
    {
        /// <summary>Adds an order to the pending queue.</summary>
        public void Enqueue(object order)
        {
            throw new NotImplementedException();
        }
    }
}
